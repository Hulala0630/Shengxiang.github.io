{
  "name": "hu-shengxiang-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:content": "node scripts/sync-projects.mjs",
    "sync:assets": "node scripts/sync-public-assets.mjs",
    "prepare:site": "npm run sync:content && npm run sync:assets",
    "dev": "npm run prepare:site && astro dev",
    "build": "npm run prepare:site && astro build",
    "preview": "astro preview",
    "check:content": "node scripts/check-projects.mjs"
  },
  "dependencies": {
    "animejs": "^4.5.0",
    "astro": "7.1.3"
  }
}
