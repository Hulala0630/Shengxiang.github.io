# Website Rebuild Specification

> 更新时间：2026-07-24  
> 工作分支：`websiteRebuild`  
> 参与者：用户、Claude、Codex

## 1. 本轮共识

网站从“平均展示所有项目的作品集”调整为“以已完成成果建立可信度，以进行中项目展示方向，其他项目按需展开的工业技术主页”。

核心叙事：

> 把工业知识变成可运行、可建模、可测试、可监视的系统。

页面必须让访客在首屏快速理解定位，并能通过轻量交互直接体验工程思路。互动服务于说明能力，不抢夺正常浏览。

## 2. Hero 交互

- Hero 默认以数字孪生单元的模拟动画自动运行。
- 默认状态不要求用户学习玩法，也不会阻塞阅读。
- 用户主动点击“进入交互 / Take control”后，才显示控制项并允许故障注入与复位。
- 用户可随时返回自动播放。
- 自动播放展示循环、产出、信号校验、孪生同步等状态。
- 必须尊重 `prefers-reduced-motion`。

## 3. 代表成果与进行中项目

2026-07-24 用户进一步确认：自动仿真生成与闭环 Agent 尚未完成，不应与已落地成果并列称为“旗舰项目”。首页改为两层：

### Proven Work

已完成内容负责建立可信度，明确区分交付类型：

1. BMW 工业错误仿真培训平台：已交付企业项目。
2. 工业数字孪生演示系统：完整个人技术原型。
3. 基于视觉的人体动作评估与反馈：完成研究项目。

### Building Now

未完成内容负责展示技术方向，必须明确标注 Prototype / In Development：

1. Unity + Isaac Sim 自动建模工具链。
2. Industrial Diagnostics Agent 与未来仿真闭环。

页面中的相关 HTML 交互标注为 Concept Preview 或 Recorded Trace，不暗示真实生产闭环已经完成。

### BMW 工业错误仿真培训平台

- 属性：企业级旗舰项目。
- 价值：将难以复现、成本高昂的真实故障转化为可控、可重复的培训场景。
- 交互：选择传感器占用、输送线卡滞或驱动器过载，观察 PLC、OPC UA、HMI 链路状态和诊断结果。
- 强调：多层故障注入、培训场景复用、减少对真实设备停机的依赖。

### Unity 数字孪生 + Isaac Sim 自动建模（Building Now）

- 属性：自动建模工具链。
- 价值：从 JSON/YAML 配置生成产线结构、设备层级、USD 场景和合成数据。
- 交互：切换 Compact Cell / Assembly Line 配置，观察配置、节点数量与场景结构同步变化。
- 强调：配置驱动、Unity/Isaac Sim 链路、异常场景与数据导出的可重复生成。

### Industrial Agent（Building Now）

- 属性：工业智能体旗舰项目。
- 价值：让 Agent 理解场景意图、调用工程工具，并对模型状态与故障执行可追踪推理。
- 交互：运行一次脚本化诊断，逐步展示 Plan、Tool、Review 与结构化结论。
- 强调：不接入真实 LLM；演示重点是工具编排、证据链和执行轨迹。

## 4. 其他项目的信息架构

- 其他项目收拢为“企业项目”和“个人项目”两个栏目。
- 默认只显示：类别、标题、简短介绍。
- 展开后必须完整显示原数据中的 pitch、problem、solution、overview、highlights、interaction、system idea、structure、result、note、technologies 和视频；不得为了首页简洁而删除原始信息。
- 继续复用 `projects.js` 的双语数据，避免丢失 Claude 与用户已整理的内容。
- 视频使用 `preload="metadata"`，仅在用户主动播放后加载播放内容。

## 5. 语言与视觉

- 首次访问默认中文，可切换英文。
- 视觉继续采用工程图纸 + 运行屏语言：
  - 页面：浅色纸张、墨色线框、深蓝灰。
  - 运行界面：深色屏幕、运行绿、报警琥珀。
  - 紫色只用于 AI 推理与 Agent 输出。
- 保留响应式布局、键盘可达性、合理触控尺寸与降低动态效果支持。

## 5.1 参考站点取长补短

参考 `https://prasadghegde.github.io/` 的优势：

- 一句话定位清晰。
- 项目包含 Problem / Architecture / Impact 和量化证据。
- 经历、教育、技能与项目形成完整职业叙事。
- 已部署项目和互动模块均有明确上下文。

不复制的部分：

- 不做全站深色高密度界面。
- 不加入启动动画、定制鼠标或为了效果而存在的重型 3D。
- 不把未完成原型包装成已落地成果。

## 6. 实现决策

2026-07-27 用户确认开始渐进迁移到 Astro，但三个重点项目保留为后续逐项共同审核任务。本轮先迁移页面壳层、数据单源、双语路由与现有 Hero，不在迁移过程中擅自定稿项目内容。现有 `docs/rebuild-spec.md` 作为 Claude 历史规格档案；`communication.md` 作为用户、Claude、Codex 的当前协作基准。本文件继续记录最新用户裁决，冲突时以本文件为准。

后续若需要独立项目 URL、SEO 内容页或更复杂的 demo islands，再评估 Astro 迁移。

## 7. 验收条件

- Hero 自动播放且不要求用户操作。
- 点击进入交互后可注入故障、复位、退出交互。
- 三个旗舰项目的 HTML 交互均可独立完成一次操作。
- 企业/个人栏目能正确筛选并展开现有项目。
- 中文/英文切换同步更新页面主文案与项目内容。
- 桌面和移动端均无横向溢出，且降低动态效果设置有效。

## 8. 2026-07-24 双视觉方案

根据用户进一步反馈，先交付两个可独立比较的 HTML，而不是过早锁定唯一主题：

### A. Bauhaus / Editorial

- 文件：`bauhaus-rebuild.html`
- 延续原网站的明亮包豪斯、编辑式网格和强几何构成。
- 使用新信息结构：个人信息与经历优先，能力列表后置。
- 不提供全站暗色主题，保持单一而统一的视觉语言。

### B. Dual-theme Technology

- 文件：`tech-dual-theme.html`
- 使用 Claude 讨论中的 DESIGN / RUNTIME 科技语言。
- Light / Dark 切换必须改变全站 token、卡片、图纸、文字与界面层级，并使用 localStorage 保存。
- 运行绿、报警琥珀、AI 紫保持稳定语义。

### 共用 Hero

- 两版共用 `assets/hero/HERO_GAME_ASM_FLUID_SIM_v2.0.html`。
- 原型来源：用户指定的 `HERO_GAME_ASM_FLUID_SIM_v2.0.html`。
- Three.js r128 已复制到 `assets/vendor/three.r128.min.js`，避免 CDN 失败导致 Hero 空白。
- 两版只负责提供展示窗口；玩法代码保持单一来源，后续共同打磨时同步生效。

## 9. 2026-07-24 包豪斯双主题布局验证

文件：`bauhaus-dual-theme-layout.html`

依据用户手绘结构调整为：

1. 首屏左上：姓名、定位、联系方式等基本信息。
2. 首屏左下：教育与工作经历，允许独立纵向滚动。
3. 首屏右侧：完整装配机器人 3D 交互模块。
4. 第二层：三个重点交互 Demo。
5. 第三层：其他项目折叠档案。
6. 最后一层：能力范围与联系方式。

三个重点 Demo 固定为：

- Industrial Fault Simulation & Training Platform。
- Unity + Isaac Sim Automatic Modeling。
- Vision-based Motion Evaluation。

不在重点 Demo 区增加“未完成项目”说明或状态营销；其他未选项目继续按档案信息处理。

主题规则：

- Light：包豪斯印刷品 / 米白纸面。
- Dark：工业制图板 / 黑色编辑界面。
- 红、蓝、黄在两种主题下保持包豪斯构成色。
- 运行绿、报警琥珀、AI 紫只服务系统状态与交互语义。
- 主题选择写入 localStorage。

## 10. 2026-07-25 横向案例叙事调整

- 教育与工作经历恢复为横向滑动轨道：
  - 使用浏览器原生横向滚动与 scroll-snap；
  - 隐藏传统 scrollbar，以左右箭头和底部进度线提示可滑动方向；
  - 鼠标、触控横划和按钮均可操作。
- Hero 不再以固定高度原样嵌入：
  - Hero 子页面通过嵌入模式移除外围留白；
  - 使用 ResizeObserver 向首页报告真实内容高度，消除机器人操作区下方空占位；
  - 首页明暗主题通过 postMessage 同步给 Hero；
  - Hero 顶栏、模式、故障按钮、提示面板与包豪斯红黄蓝及暗色变量统一。
- 三个旗舰项目改为纵向排列的横板案例：
  - 左侧为一句话价值、项目定位与关键点；
  - 右侧保留一次可理解的 HTML 交互；
  - 底部显示交互结果，并可展开完整背景、方法与边界；
  - Isaac Sim Generator / Data Agent 的展开区使用 `DemoIsaacSimGenerator.mp4`。
- 其他项目默认展示项目类型、一句话简介和效果预览：
  - 有视频的项目使用静音悬停预览；
  - 展开后显示问题、方案、结果、技术标签与完整视频；
  - 无视频项目使用技术结构示意作为效果占位，而不是空白区域。

## 11. 2026-07-25 首屏对齐与导航收敛

- 首屏个人信息/横向履历与 Hero 采用同一顶部和底部基线。
- 桌面端根据左栏实际高度动态调整 Hero viewport，高度用于场景本身，不产生空占位。
- 新增左侧纵向导航：
  - 支持上一段、下一段和四个主章节直达；
  - 当前章节自动高亮；
  - 导航可隐藏并通过边缘按钮重新打开；
  - 手机端默认收起，避免遮挡正文。
- 顶部导航链接移交给左侧导航，顶部只保留身份、主题和语言。
- 删除可见的 Demo 编号、Three Demos、View 03 Demos 和初始 READY 提示。
- Hero 嵌入模式隐藏拖拽说明、比较步骤和快捷键提示，仅保留系统状态与真实控制。
- 项目交互结果只在访客执行操作后出现。

## 12. 2026-07-27 Astro 渐进迁移裁决

- 当前 Bauhaus 双主题方向作为 Astro 迁移视觉基准。
- 本轮迁移范围：
  - 页面壳层与设计 Token；
  - 中文默认 `/` 和英文 `/en/`；
  - 横向履历、侧栏导航、主题和现有 Hero iframe；
  - 从 `projects.js` 同步并校验的统一项目数据；
  - 完整项目档案。
- 三个重点项目只迁移事实数据和展示位置，不在本轮定稿文案或交互。
- 三个重点项目必须由用户、Claude、Codex 后续逐个审核和共同完成。
- 本地三方校验完成前：
  - 不提交 Git；
  - 不推送远程；
  - 不切换正式线上入口。
- `communication.md` 是三方协作基准，必须区分 `[USER]`、`[CODEX]`、`[CLAUDE]` 与 `[SHARED]`。
